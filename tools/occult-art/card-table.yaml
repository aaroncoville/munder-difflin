# The tasks parlour — the round baize table the kanban counts sit on.
#
# The sheet the shipped panel was generated from. Reproduce it with the call in
# this directory's README; the reference image is identified by digest rather
# than by name because the request carried it inline.
panel: src/renderer/src/scene/study/assets/room-card-table.png
model: black-forest-labs/flux-kontext-pro
input:
  prompt: |-
    Repaint as a quiet parlor: keep the warm red paneled walls, chandeliers and lamps, remove the stag heads and the fireplace, place one large round table with a green baize top at the center, small rectangular paper notes arranged in tidy rows on its surface, a few simple wooden chairs around it, bookshelves at the sides. Render as FLAT 2D storybook game illustration: naive simple shapes, matte flat colors with soft paper-grain texture, NO realistic lighting, NO 3D rendering, NO photorealism. Straight-on elevation. No people, no animals, no readable text.
  aspect_ratio: "21:9"
  output_format: png
  input_image: "the reference below, supplied inline as a data: URI"
reference:
  media_type: image/jpeg
  bytes: 253739
  sha256: 00c6ff71bd5c92d23cabc060a36846d79f73ba05b73a7ad772a20c5407782961
output:
  width: 1568
  height: 672
